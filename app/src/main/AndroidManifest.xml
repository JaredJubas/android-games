<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="group0681phase2.gamecenter">

    <uses-permission android:name="android.permission.INTERNET" />
    uses-permission>

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".GameLaunchCentre"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StartingActivity" />
        <activity android:name=".GameActivity" />
        <activity android:name=".ScoreboardActivity" />
        <activity android:name=".LoadBackgroundActivity" />
        <activity android:name=".SizeSelectionActivity" />
        <activity android:name=".PlayerSelectionActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login_sign_up" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ScoreboardList" />
        <activity android:name=".GameSelectActivity" />
        <activity android:name=".SimonGameActivity" />
        <activity android:name=".TicGameActivity" />
        <activity android:name=".SimonMenuActivity" />
        <activity android:name=".SimonScoreboardActivity" />
        <activity android:name=".InstructionsActivity" />
        <activity android:name=".ComplexityActivity"></activity>
    </application>

</manifest>